services:
  veilnet-conflux:
    build: .
    container_name: veilnet-conflux
    image: veilnet/conflux:Beta-v1.0.3
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    env_file:
      - .env
